name: Reusable Generated Dart Preparation

on:
  workflow_call:
    inputs:
      source-ref:
        description: Optional source ref to generate from instead of the event default ref
        required: false
        default: ''
        type: string
    outputs:
      artifact-name:
        description: Generated Dart artifact later jobs
        value: ${{ jobs.prepare.outputs.artifact-name }}
      source-sha:
        description: Source SHA used to generate the artifact
        value: ${{ jobs.prepare.outputs.source-sha }}

jobs:
  prepare:
    name: Prepare Generated Dart
    runs-on:
      - self-hosted
      - linux
      - flutter-3.41.9
    permissions:
      contents: read
    outputs:
      artifact-name: ${{ steps.source.outputs.artifact-name }}
      source-sha: ${{ steps.source.outputs.source-sha }}
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ inputs.source-ref }}

      - name: Resolve checked out source
        id: source
        shell: bash
        run: |
          source_sha="$(git rev-parse HEAD)"
          echo "source-sha=${source_sha}" >> "${GITHUB_OUTPUT}"
          echo "artifact-name=generated-dart-${source_sha}-${GITHUB_RUN_ID}" >> "${GITHUB_OUTPUT}"

      - name: Verify Flutter setup
        uses: ./.github/actions/flutter-ci-setup

      - name: Generate Dart sources
        working-directory: Flutter/budgetit
        run: dart run build_runner build --delete-conflicting-outputs

      - name: Upload generated Dart artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: ${{ steps.source.outputs.artifact-name }}
          path: |
            Flutter/budgetit/lib/**/*.g.dart
            Flutter/budgetit/lib/**/*.freezed.dart
            Flutter/budgetit/lib/**/*.mocks.dart
            Flutter/budgetit/test/**/*.g.dart
            Flutter/budgetit/test/**/*.freezed.dart
            Flutter/budgetit/test/**/*.mocks.dart
          if-no-files-found: error
